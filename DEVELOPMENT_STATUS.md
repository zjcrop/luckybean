# LuckyBean 1.23D — 开发状态

> 后续完整开发顺序、未关闭问题和验收门禁统一见 `docs/LuckyBean-1.23D-DEVELOPMENT-HANDBOOK.md`。本文件记录当前检查点，不代表历史业务问题已经全部关闭。

日期：2026-08-08  
基线：GitHub `zjcrop/luckybean` main，提交 `ebcd1b7`  
当前：LuckyBean 1.23D-main-deploy 候选（待 GitHub Actions、Pages 与 APK 构建）

## 当前版本边界

- `main` 是唯一开发与测试主线；达到发布标准后直接标记 Release。
- 暂不推进 2.0、Compose、Room 或 KMP，先在 1.2.3 行为基线上完成 1.23D 的问题修复与验收。
- Web/PWA 继续使用 IndexedDB；既有 Android 测试壳随 1.23D Web 版本验证。
- 业务规则、数据契约、编码表与测试基准保持平台无关，为未来原生化预留条件。
- BrewProfiles 继续提供私有服务端权威计算；LuckyBean 本地负责缓存、离线降级、执行与 3D 渲染。计算接口不依赖云同步账户 JWT。
- App 不设置第二套账户。只保留服务器同步登录，登录后自动同步。

## 本检查点已完成

1. 从 1.2.3 发布包恢复 Web/PWA 行为基线，并核对发布 SHA-256。
2. 数据 Schema 从 7 升至 8，应用显示版本回正为 `1.23D`。
3. 新增 `.luckybean` 可移植归档契约 v1：
   - 覆盖豆卡、冲煮、品鉴、库存、设置、自定义编码、编码表缓存、分享草稿、历史修订、回收站与待同步队列。
   - 每个数据区块独立 SHA-256；整包另有 SHA-256。
   - 不导出服务器登录会话与本机密钥；恢复时在新设备上重新加密隐私数据。
4. 新增旧版 `luckybean-backup` JSON 到 `.luckybean` v1 的兼容迁移。
5. 恢复流程改为：完整解析 → 安全结构检查 → 版本检查 → 哈希检查 → 数量预览 → 用户确认 → 单个 IndexedDB 事务替换。
6. PWA 离线资源表已纳入归档与结构化识别模块，版本缓存已切换到 1.23D。
7. 删除 `productionDate → roastDate` 回填和识别结果默认当天两条错误规则。
8. 建立结构化 RecognitionDocument、日期候选分类、硬排除规则及用户确认界面。
9. 将日期确认决策抽为可测试领域模块；确认页展示全部候选并强制烘焙日期单选。
10. 修复“已绑定日期的标签向下一 OCR 行泄漏”问题，避免 Best Before/MFG 等标签污染后续批次日期。
11. 用户确认保留原 OCR 置信度与图片/区块来源；人工确认不再伪装成 OCR 置信度 100%。
12. 新增独立 `recognition-test.html` 日期归属 Web 验收页并加入离线缓存。
13. 将 1.23D 改动移植到远端最新 main，保留独立品鉴状态机、器具模型、分享地址和既有主线回归测试。
14. 恢复 Android WebView 测试壳，并为导出文件接入系统 `ACTION_CREATE_DOCUMENT` 保存流程。
15. 将 Web、PWA、Android、Actions 和构建产物版本统一为 `1.23D`；Android `versionCode` 为 `102304`。

## 已通过验证

- 归档完整往返：通过。
- 文件内容被篡改时拒绝导入：通过。
- 1.2.x 旧备份迁移：通过。
- 高于当前 Schema 的未来备份拒绝导入：通过。
- 全部 JavaScript 文件语法检查：通过。
- 本地模块引用与 Service Worker 离线资源路径检查：通过。
- HTTP 静态入口、manifest、归档模块与 JSON Schema 可访问性检查：通过。
- 日期字段专项、确认决策与首批 30 组黄金样本：49 项测试全部通过。
- Web 验收页、确认模块与分类器 HTTP 静态入口可访问；66 项离线资源路径完整。
- main 原有静态测试、BrewProfiles 真实服务合约、1.23D 部署契约及 49 项专项测试通过。
- `npm audit --audit-level=high`：0 个漏洞。

## 当前识别检查点验证

已通过代码级回归：生产日期、包装日期、最佳赏味期、到期日不再进入烘焙日期；无标签日期要求确认；明确烘焙标签可以自动填入；无效日历日期被拒绝。

用户已决定跳过 100 组真实豆袋收集作为本次发布门禁；该项保留为后续抽样质量改进，不标记为已完成。仍需由 GitHub Actions 完成 Chromium 交互回归、Pages 部署和 Android APK 构建，并在真实 Android WebView 上验证相机、文件与语音。

远端 main 已有推荐、分享、品鉴、器具、同步和 BrewProfiles 的自动回归基础；本次部署后仍需继续做真机与跨设备验收，不能仅凭代码存在标记全部关闭。

## 未使用素材

本轮附带的两张人物照片未进入应用、测试包或归档，待项目对其用途有明确要求后再处理。
