# LuckyBean 1.23E — 开发状态

日期：2026-08-13  
发布修订：`1.23E-main-sync.7`  
Android：`versionCode 102318` / `versionName 1.23E`

## main-sync.7 完整修改范围

- 冲煮优化只在完成冲煮并完成品鉴后评估；总分、预测分及两者差值均不能单独触发。
- 触发依据为同组均值明显偏低的专业雷达维度，或明确记录的香气、酸、甜、苦、涩、醇厚度、干净度、平衡度等负面项目。
- 优化固定沿用实际冲煮方案，只重新计算温度、分段水量、流速、时间等可控参数；服务端计算失败时不生成本地权威替代结果。
- 建议和原因写回源豆卡，可一键载入下次冲煮；下次品鉴后自动标记有效、部分有效、未改善或结果不确定，原记录保持不变。
- 计算协调器采用 latest-wins，旧并发结果不能覆盖新结果；同豆卡参数重算保持已采用方案，减少重复匹配和界面抖动。
- 3D 保留确定性的模型预测曲线用于趋势和参数影响观察；标准靶区与个人感知靶区分层，个人层至少需要三次关联品鉴证据。
- 3D 渲染失败只允许对相同场景与计划指纹重新渲染，不回退为表格或另造预测。
- 自动化门禁覆盖语法、维度触发、方案保持、并发 latest-wins、个人靶区证据阈值、离线缓存、APK 资源、私钥泄漏扫描及官方证书 SHA-256 校验。
- 正式 APK 只通过 GitHub Actions 中的发布密钥签名；仓库和 APK 均不得包含私钥、keystore Base64 或服务端密钥。

以下为此前检查点历史记录。

> 后续完整开发顺序、未关闭问题和验收门禁统一见 `docs/LuckyBean-1.23D-DEVELOPMENT-HANDBOOK.md`。本文件记录当前检查点，不代表历史业务问题已经全部关闭。

日期：2026-08-10
唯一主线：GitHub `zjcrop/luckybean` `main`

当前部署：LuckyBean `1.23D`，唯一来源为 GitHub `main` 的最新成功部署提交。

内部资源修订：`1.23D-main-sync.5`（仅用于清除旧 PWA/WebView 缓存，不是第二条版本线）

## 当前版本边界

- `main` 是唯一开发与测试主线；达到发布标准后直接标记 Release。
- 暂不推进 2.0、Compose、Room 或 KMP，先在 1.2.3 行为基线上完成 1.23D 的问题修复与验收。
- Web/PWA 继续使用 IndexedDB；既有 Android 测试壳随 1.23D Web 版本验证。
- 业务规则、数据契约、编码表与测试基准保持平台无关，为未来原生化预留条件。
- BrewProfiles 继续提供私有服务端权威计算；LuckyBean 本地负责缓存、离线降级、执行与 3D 渲染。计算接口不依赖云同步账户 JWT。
- App 不设置第二套账户。只保留服务器同步登录，登录后自动同步。

## 本检查点已完成

1. 从 1.2.3 发布包恢复 Web/PWA 行为基线，并核对发布 SHA-256。
2. 数据 Schema 从 7 升至 8，应用显示版本回正为 `1.23D`。
3. 新增 `.luckybean` 可移植归档契约 v1：
   - 覆盖豆卡、冲煮、品鉴、库存、设置、自定义编码、编码表缓存、分享草稿、历史修订、回收站与待同步队列。
   - 每个数据区块独立 SHA-256；整包另有 SHA-256。
   - 不导出服务器登录会话与本机密钥；恢复时在新设备上重新加密隐私数据。
4. 新增旧版 `luckybean-backup` JSON 到 `.luckybean` v1 的兼容迁移。
5. 恢复流程改为：完整解析 → 安全结构检查 → 版本检查 → 哈希检查 → 数量预览 → 用户确认 → 单个 IndexedDB 事务替换。
6. PWA 离线资源表已纳入归档与结构化识别模块，版本缓存已切换到 1.23D。
7. 删除 `productionDate → roastDate` 回填和识别结果默认当天两条错误规则。
8. 建立结构化 RecognitionDocument、日期候选分类、硬排除规则及用户确认界面。
9. 将日期确认决策抽为可测试领域模块；确认页展示全部候选并强制烘焙日期单选。
10. 修复“已绑定日期的标签向下一 OCR 行泄漏”问题，避免 Best Before/MFG 等标签污染后续批次日期。
11. 用户确认保留原 OCR 置信度与图片/区块来源；人工确认不再伪装成 OCR 置信度 100%。
12. 新增独立 `recognition-test.html` 日期归属 Web 验收页并加入离线缓存。
13. 将 1.23D 改动移植到远端最新 main，保留独立品鉴状态机、器具模型、分享地址和既有主线回归测试。
14. 恢复 Android WebView 测试壳，并为导出文件接入系统 `ACTION_CREATE_DOCUMENT` 保存流程。
15. 将 Web、PWA、Android、Actions 和构建产物版本统一为 `1.23D`；Android `versionCode` 为 `102304`。
16. 修复正式 `brew-analysis/2.0` 分析包络与旧方案 `schemaVersion` 校验冲突；正式分析仍要求统一计算指纹、引擎版本、方案版本、有效阶段和水量守恒。
17. 修复设置页红色/白色启动图预览背景；HTML 属性、交互选择器和计算样式已统一。
18. 更新 Service Worker 缓存名和全部资源查询修订号，已安装 PWA 与 Android WebView 不再复用修复前资源。
19. 云同步改为双端并集合并；首次登录保留两端全部数据，建立同步基线后本地删除会传播到服务器。
20. 同步分包改为内容寻址，Manifest 提交使用服务器完成时间条件更新，避免两个设备同时提交时静默覆盖。
21. Supabase Manifest 新增服务器生成的 `sync_completed_at`，Manifest/Chunk 的 `uploaded_at` 也由数据库触发器写入；RLS 与四类用户级策略保持启用。
22. Android 豆袋识别改为 APK 内置 ML Kit 中文与拉丁模型，不再依赖手机运行时动态加载 PaddleOCR CDN。
23. 修复移动端杯测和品鉴雷达浮层的纵向滚动及底部安全区。
24. 恢复“风味喜好数字测写”和“咖啡世界”，世界地图资源随 Web/PWA/Android 本地打包；“本物”插图改为透明背景、无边框并缩至页面宽度三分之一。
25. 将“风味喜好数字测写”和“咖啡世界”唯一入口移至“器设 → 数藏”；豆藏荐榜上方新增全部豆卡余量、当天实际扣豆量、咖啡因参考余量、超量与睡眠时间提醒。健康口径以库存事件为唯一消费证据，默认采用一般成人 400mg/日、阿拉比卡 12mg/g豆估算和睡前 6 小时提醒，并允许用户向下调整阈值及设置预计入睡时间。

## 已通过验证

- 归档完整往返：通过。
- 文件内容被篡改时拒绝导入：通过。
- 1.2.x 旧备份迁移：通过。
- 高于当前 Schema 的未来备份拒绝导入：通过。
- 全部 JavaScript 文件语法检查：通过。
- 本地模块引用与 Service Worker 离线资源路径检查：通过。
- HTTP 静态入口、manifest、归档模块与 JSON Schema 可访问性检查：通过。
- 日期、归档与方案契约专项：59 项测试全部通过。
- Web 验收页、确认模块与分类器 HTTP 静态入口可访问；66 项离线资源路径完整。
- GitHub Actions Chromium 已实际完成方案生成、3D 预览、红白背景计算样式与图片加载回归。
- BrewProfiles 真实服务严格契约及 6 套竞赛方案通过。
- Pages 已部署并核对 `version.json`、正式契约代码、预览资源和 Service Worker；提交指纹为 `d00edab22710113db24baeecd3e04805f101757a`。
- Android APK 已由同一提交构建；内嵌 Web 资源包含本轮两项修复。
- `npm audit --audit-level=high`：0 个漏洞。
- `npm run test:static`：同步、部署、旧功能、移动滚动和识别契约全部通过。
- Supabase 时间戳触发器已用事务内回滚更新验证：`uploaded_at` 为服务器当前时间，且 `sync_completed_at = uploaded_at`。
- 当前工作环境未安装 Chromium 与 Gradle；浏览器端到端和 APK 编译由推送后的 GitHub Actions 执行，真机 OCR/滚动仍需安装新 APK 验收。

## 当前识别检查点验证

已通过代码级回归：生产日期、包装日期、最佳赏味期、到期日不再进入烘焙日期；无标签日期要求确认；明确烘焙标签可以自动填入；无效日历日期被拒绝。

用户已决定跳过 100 组真实豆袋收集作为本次发布门禁；该项保留为后续抽样质量改进，不标记为已完成。仍需在真实 Android WebView 上验证相机、系统文件保存与语音。

远端 main 已有推荐、分享、品鉴、器具、同步和 BrewProfiles 的自动回归基础；仍需继续做真机与跨设备验收，不能仅凭代码或 CI 存在标记全部关闭。

## 防止重复修复

- 本轮业务范围仅为：正式计算方案契约兼容、红白启动图预览背景。
- 后续不得重新引入旧 `schemaVersion` 测试桩，不得把内部缓存修订号解释为新产品版本。
- 已通过的识别、归档、日期规则和 Android 文件导出不重复改写；只有新的可复现失败证据才能重新开启。
